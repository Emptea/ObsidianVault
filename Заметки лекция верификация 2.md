- `mailbox.get()` блокирует дальнейшее выполнение процесса до получения пакета
- `mailbox` работает только как FIFO
- при наличии `tvalid` и `tready` наличие 1 на обоих называется "рукопожатием"(handshake)
- рекомендация AXI-Stream - `tready = 1` по умолчанию